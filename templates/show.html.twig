{% extends 'layout.html.twig' %}
{% block titre %}
show
{% endblock %}
{% block content %}
                <h3>Cakes show</h3>
                <div class="row">
                <div class="col-sm-3">
<a href="cake.php?id={{ cake.id }}">
    <article class="card">
    <img class="card-img-top" src="{{ cake.image }}" alt="{{ cake.name }}" style="height:300px">
        <div class="card-body">
            <p class="card-text">{{ cake.name }} - {{ cake.price }} €</p>
        </div>
    </article>
</a>
                </div>
                </div>
{% endblock %}